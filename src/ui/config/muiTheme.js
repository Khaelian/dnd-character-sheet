import {createMuiTheme} from '@material-ui/core'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#009900',
    }
  },
  spacing: {
    small: 3,
    medium: 8,
    large: 20,
  }
})

export default theme
